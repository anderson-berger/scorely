/**
 * Core HTTP client — thin re-export of the configured Axios instance.
 * All modules should import from here, never from services/api/api directly.
 */
export { default } from 'src/services/api/api';
